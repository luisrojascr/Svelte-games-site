query GetCalendar($month: Int!) {
	getCalendar(month: $month) {
		month
		startDay
		endDay
		daysOpened {
			day
			title
			description
			hasEligibility
			minDeposit
			claimedAt
			claimedEligibleAt
			freeSpins
		}
	}
}
