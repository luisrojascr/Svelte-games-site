query GetCalendar($month: Int!) {
	getCalendar(month: $month) {
		month
		startDay
		endDay
		daysOpened {
			month
			day
			title
			description
			disclaimer
			hasEligibility
			minDeposit
			claimedAt
			claimedEligibleAt
			freeSpins
		}
	}
}
