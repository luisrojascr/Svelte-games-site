{
	"name": "@infinitro/postauth-ui",
	"version": "0.1.41",
	"private": true,
	"scripts": {
		"dev:prod": "NODE_ENV=production VITE_GIT_VERSION=$(git rev-parse --short HEAD) VITE_APP_VERSION=$npm_package_version env-cmd -f .env.prod.env vite dev",
		"dev:stage": "NODE_ENV=production VITE_GIT_VERSION=$(git rev-parse --short HEAD) VITE_APP_VERSION=$npm_package_version env-cmd -f .env.stage.env vite dev",
		"dev:local": "NODE_ENV=production VITE_GIT_VERSION=$(git rev-parse --short HEAD) VITE_APP_VERSION=$npm_package_version env-cmd -f .env.local.env vite dev",
		"build:prod": "NODE_ENV=production VITE_GIT_VERSION=$(git rev-parse --short HEAD) VITE_APP_VERSION=$npm_package_version env-cmd -f .env.prod.env vite build",
		"build:stage": "NODE_ENV=production VITE_GIT_VERSION=$(git rev-parse --short HEAD) VITE_APP_VERSION=$npm_package_version env-cmd -f .env.stage.env vite build",
		"build:local": "NODE_ENV=production VITE_GIT_VERSION=$(git rev-parse --short HEAD) VITE_APP_VERSION=$npm_package_version env-cmd -f .env.local.env vite build",
		"preview": "vite preview",
		"test": "npm run test:integration && npm run test:unit",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"codegen": "graphql-codegen",
		"lint": "prettier --plugin-search-dir . --check . && eslint .",
		"format": "prettier --plugin-search-dir . --write .",
		"test:integration": "playwright test",
		"test:unit": "vitest",
		"upload:stage": "cd ../../libs/advent-calendar && pnpm run build:prod && cd ../../websites/postauth-ui && pnpm run build:stage && aws-vault exec fe-dev -- aws s3 sync ./build s3://postauth-ui-stage.fe-dev.infinitro.dev-a552a30 && aws-vault exec fe-dev -- aws cloudfront create-invalidation --distribution-id E3O1LJ5X2SB4FA --paths '/*'",
		"upload:prod": "cd ../../libs/advent-calendar && pnpm run build:prod && cd ../../websites/postauth-ui && pnpm run build:prod && aws-vault exec fe-dev -- aws s3 sync ./build s3://postauth-ui.fe-dev.infinitro.dev-0609b88 && aws-vault exec fe-dev -- aws cloudfront create-invalidation --distribution-id E2RKFCXO6C5UQ5 --paths '/*'"
	},
	"devDependencies": {
		"@graphql-codegen/cli": "^5.0.0",
		"@graphql-codegen/typescript": "^4.0.1",
		"@graphql-codegen/typescript-operations": "^4.0.1",
		"@playwright/test": "^1.28.1",
		"@sveltejs/adapter-static": "^2.0.3",
		"@sveltejs/kit": "^1.20.4",
		"@typescript-eslint/eslint-plugin": "^6.0.0",
		"@typescript-eslint/parser": "^6.0.0",
		"autoprefixer": "^10.4.16",
		"env-cmd": "^10.1.0",
		"eslint": "^8.28.0",
		"eslint-config-prettier": "^8.5.0",
		"eslint-plugin-svelte": "^2.30.0",
		"graphql-codegen-svelte-apollo": "^1.1.0",
		"postcss": "^8.4.31",
		"prettier": "^2.8.0",
		"prettier-plugin-svelte": "^2.10.1",
		"svelte": "^4.0.5",
		"svelte-check": "^3.4.3",
		"tailwindcss": "^3.3.3",
		"tslib": "^2.4.1",
		"typescript": "^5.0.0",
		"vite": "^4.4.2",
		"vitest": "^0.32.2"
	},
	"type": "module",
	"dependencies": {
		"@infintro/advent-calendar": "link:../../libs/advent-calendar"
	}
}
